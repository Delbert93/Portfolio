@page "/platforms/{slug}"
@inject Portfolio.Blazor.ProjectApiService portfoiliApi
@inject NavigationManager navManager

<DisplayProjectsPlatforms Slug="@Slug" />

@code {
    [Parameter]
    public string Slug { get; set; }
}