@page "/languages/{slug}"
@inject Portfolio.Blazor.ProjectApiService portfoiliApi
@inject NavigationManager navManager

<DisplayProjects Slug="@Slug"/>

@code {
    [Parameter]
    public string Slug { get; set; }
}
