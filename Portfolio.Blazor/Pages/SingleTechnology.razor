@page "/technologies/{slug}"
@inject Portfolio.Blazor.ProjectApiService portfoiliApi
@inject NavigationManager navManager

<DisplayProjectsTechnologies Slug="@Slug" />

@code {
    [Parameter]
    public string Slug { get; set; }
}